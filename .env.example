# REQUIRED: Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=your-secret-key-here-generate-with-python-command-above

# DEBUG=False in production, DEBUG=True in development
DEBUG=True

ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (PostgreSQL - for production)
# Leave DB_HOST empty to use SQLite (development)
DB_HOST=
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_PORT=5432
